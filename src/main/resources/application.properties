# Load local environment variables when a backend .env file exists.
spring.config.import=optional:file:.env[.properties]

# PostgreSQL Configuration
spring.datasource.url=jdbc:postgresql://dpg-daejk1id0e5s738p7rug-a.singapore-postgres.render.com:5432/moneymanger_evt6?sslmode=require
spring.datasource.username=moneymanger_evt6_user
spring.datasource.password=${DATABASE_PASSWORD}

server.servlet.context-path=/api/v1.0

#JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Email configuration
spring.mail.host=smtp-relay.brevo.com
spring.mail.port=587
spring.mail.username=${BREVO_USERNAME}
spring.mail.password=${BREVO_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.protocol=smtp
spring.mail.properties.mail.smtp.from=${BREVO_FROM_EMAIL}
jwt.secret=2b8e7c7e-4e2a-4b1a-9c2e-8f7e2d3c4b5a
money.manager.frontend.url=${MONEY_MANAGER_FRONTEND_URL}
app.activation.url=${MONEY_MANAGER_BACKEND_URL}
