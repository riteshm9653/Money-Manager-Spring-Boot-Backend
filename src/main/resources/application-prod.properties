# ======================
# Server
# ======================
server.servlet.context-path=/api/v1.0
server.port=${PORT:8080}

# ======================
# PostgreSQL (Render)
# ======================
spring.datasource.url=jdbc:postgresql://dpg-daejk1id0e5s738p7rug-a.singapore-postgres.render.com:5432/moneymanger_evt6
spring.datasource.username=moneymanger_evt6_user
spring.datasource.password=KdvQxk145kAQo8IQXX4ciqjwhuGMMnWy
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.open-in-view=false

# ======================
# Email (using Environment Variables)
# ======================
spring.mail.host=smtp-relay.brevo.com
spring.mail.port=587
spring.mail.username=${BREVO_USERNAME}
spring.mail.password=${BREVO_PASSWORD}
spring.mail.protocol=smtp
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.from=${BREVO_FROM_EMAIL}

# ======================
# JWT
# ======================
jwt.secret=${JWT_SECRET:2b8e7c7e-4e2a-4b1a-9c2e-8f7e2d3c4b5a}

# ======================
# Application URLs
# ======================
money.manager.frontend.url=${MONEY_MANAGER_FRONTEND_URL}
app.activation.url=${MONEY_MANAGER_BACKEND_URL}